# rengfx

RE ENGINE FX

## hacking

requirements:
+ `dub` and a D compiler (`dmd`, `gdc`, `ldc`)

build:
```sh
cd src
dub test # run tests
dub build # build library
cd ../demo/basic
dub run # run demo
```
